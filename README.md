
  # ERP_Sanjeevan (Community)

  This is a code bundle for ERP_Sanjeevan (Community). The original project is available at https://www.figma.com/design/RBjf60EPT9o1zxJbsnTX70/ERP_Sanjeevan--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  ## cd /backend 
    Run "pip install uvicorn fastapi"

    Run "python -m pip install uvicorn fastapi"

    Run "python -m uvicorn main:app --reload"
